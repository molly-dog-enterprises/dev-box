# Molly dog productions development box

A Vagrant box for molly dog application development.

# Install

```bash
$ vagrant up
$ vagrant provision # may need to run twice for postgres to install properly
$ vagrant ssh
```
